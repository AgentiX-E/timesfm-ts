{
  "name": "@agentix-e/timesfm-hierarchical",
  "version": "0.3.1",
  "description": "Hierarchical time series reconciliation for TimesFM — bottom-up, OLS, WLS, and MinT strategies",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "prepublishOnly": "tsc"
  },
  "dependencies": {
    "@agentix-e/timesfm-core": "workspace:*",
    "ml-matrix": "^6.12.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "keywords": [
    "timesfm",
    "hierarchical",
    "reconciliation",
    "time-series",
    "forecasting",
    "mint",
    "bottom-up"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/AgentiX-E/timesfm-ts"
  },
  "publishConfig": {
    "access": "public"
  }
}
